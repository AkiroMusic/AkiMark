{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AkiMark default capability",
  "windows": ["overlay", "settings"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
